version: '3.8'

services:
  postgres:
    image: postgres:16-alpine
    ports:
      - '5432:5432'
    environment:
      POSTGRES_USER: saahvik
      POSTGRES_PASSWORD: saahvik
      POSTGRES_DB: saahvik
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
